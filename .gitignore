# Local backups created during editing
*.backup.html

# Local secrets — never commit (API keys, etc.)
config.local.js
*.local.js

# Node
node_modules/
npm-debug.log*

# OS / editor cruft
.DS_Store
Thumbs.db
